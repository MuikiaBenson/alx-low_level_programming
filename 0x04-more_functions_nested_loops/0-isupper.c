#include "main.h"

/**
 * _isupper- checks for upper letters
 * @a: number
 * Return: 1 (upper letter) or 0 (else)
 */

int _isupper(int a)
{
	if (a >= 65 && a <= 90)
	{
	return (1);
	}
	return (0);
}
