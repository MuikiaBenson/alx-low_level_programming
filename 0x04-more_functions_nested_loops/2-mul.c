#include "main.h"

/**
 * mul- multiply 2 integers
 * @a: first integer to multiply
 * @b: second integer to multiply
 * Return: Results of the two integer multiplication
 */

int mul(int a, int b)
{
	return (a * b);
}
